interface:
  display_name: "OpenClaw Cluster Orchestrator"
  short_description: "Route cluster jobs to local worker agents"
  default_prompt: "Use $openclaw-cluster-orchestrator to route one pending publish job from docs/cluster/ to the correct local worker agent and record the cluster result."

policy:
  allow_implicit_invocation: false
